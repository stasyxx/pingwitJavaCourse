package lesson_20.classwork.Library;

import java.util.HashMap;
import java.util.Map;
import java.util.Scanner;
import java.util.UUID;

public class SignUp {
   /* private static final String FILE_WITH_NAMES = "libraryUsers.txt";

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        Map<String, String[]> usersMap = readUsersFromFile();
        System.out.print("Hi!Do you already have an account?");
        String response = scanner.nextLine();
        if (response.equalsIgnoreCase("No")) {
            System.out.println("Enter your name: ");
            String firstName = scanner.nextLine();
            System.out.println("Enter your surname: ");
            String lastName = scanner.nextLine();
            String userId = UUID.randomUUID().toString();

            String[] userNameSurname = {firstName, lastName};
            usersMap.put(userId, userNameSurname);

            writeUsersToFile(usersMap);


        }

        public static Map<String, String[]> readUsersFromFile() {
            Map<String, String[]> usersMap = new HashMap<>();

        }

        public static void writeUsersToFile(Map<String, String[]> usersMap) {


        }

    } */
}

