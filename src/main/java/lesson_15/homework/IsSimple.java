package lesson_15.homework;

import java.util.Scanner;

/*
Task 1
Создайте метод, который принимает целочисленный параметр и
возвращает логическое значение, указывающее, является ли число
простым или нет. Если параметр меньше или равен нулю, создайте исключение
IllegalArgumentException.
 */
public class IsSimple {
   /* public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter your number: ");
        int userNumber = scanner.nextInt();
    }

    public static boolean isSimple(int userNumber) {
        try {
            boolean isSimple = isSimple(userNumber);
            if (userNumber <= 1) {
            return false;
            }
        for (int i = 2; i <= Math.sqrt(userNumber); i++) {
            if (userNumber % i == 0) {
                return false;
            } else {

            } catch (Exception e) {
                throw new RuntimeException(e);
            }
            System.out.println("Your number " + userNumber + " is simple");
        }

        return true;
    }
        } */
    }
